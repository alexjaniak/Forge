[project]
name = "forge-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "forge-cli",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "apps/forge-cli",
]

[tool.uv.sources]
forge-cli = { workspace = true }
